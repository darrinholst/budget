<%= render "shared/validation_messages", :target => @budget %>

<%= form_for @budget do |budget| %>
  <%= budget.text_field :starts_on %>

  <table class="table">
    <tr>
      <th></th>
      <th>Budgeted</th>
      <th>Spent</th>
      <th>Remaining</th>
    </tr>
    <% @budget.categories.each do |category| %>
      <tr class="category">
        <td><%= category.name %></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>

      <% category.buckets.each do |bucket| %>
        <tr class="bucket">
          <td><%= bucket.name %></td>
          <td></td>
          <td></td>
          <td></td>
        </tr>
      <% end %>
    <% end %>
  </table>
<% end %>

